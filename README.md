# Система бронирования мест

[![CI](https://github.com/your-username/booking-system/workflows/CI/badge.svg)](https://github.com/your-username/booking-system/actions)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Node.js Version](https://img.shields.io/badge/node-%3E%3D25.2.1-brightgreen)](https://nodejs.org)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.3-blue)](https://www.typescriptlang.org/)

REST API для бронирования мест на мероприятия с защитой от дублирования и контролем вместимости.

## 🚀 Особенности

- ✅ **Транзакции** с пессимистическими блокировками (SELECT FOR UPDATE)
- ✅ **Защита от race conditions** при конкурентных запросах
- ✅ **Валидация** входных данных
- ✅ **Контроль вместимости** мероприятий
- ✅ **Предотвращение дубликатов** бронирований
- ✅ **Property-based тестирование** (100+ итераций на свойство)
- ✅ **Integration тесты** для end-to-end проверки
- ✅ **SOLID принципы** и чистая архитектура

## Технологии

- Node.js v25.2.1
- TypeScript
- Express.js
- PostgreSQL
- Jest + fast-check (property-based testing)

## Установка и настройка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка базы данных

Создайте базу данных PostgreSQL:

```sql
CREATE DATABASE your_name;
```

Параметры подключения (по умолчанию):
- Host: localhost
- Port: 5432
- Database: your_name
- Username: postgres
- Password: 1111

Для изменения параметров используйте переменные окружения:
- `DB_HOST`
- `DB_PORT`
- `DB_NAME`
- `DB_USER`
- `DB_PASSWORD`

### 3. Запуск миграций

```bash
npm run migrate
```

Это создаст таблицы `events` и `bookings`.

## Запуск приложения

### Development режим

```bash
npm run dev
```

Сервер запустится на порту 3000 (или PORT из переменных окружения).

### Production режим

```bash
npm run build
npm start
```

## API Endpoints

### POST /api/bookings/reserve

Создание бронирования места на мероприятие.

**Request Body:**
```json
{
  "event_id": 1,
  "user_id": "user123"
}
```

**Success Response (201):**
```json
{
  "success": true,
  "booking": {
    "id": 1,
    "event_id": 1,
    "user_id": "user123",
    "created_at": "2024-01-15T10:30:00.000Z"
  }
}
```

**Error Responses:**

**400 - Validation Error:**
```json
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "event_id and user_id are required"
  }
}
```

**404 - Event Not Found:**
```json
{
  "success": false,
  "error": {
    "code": "NOT_FOUND",
    "message": "Event with id 999 does not exist"
  }
}
```

**409 - Duplicate Booking:**
```json
{
  "success": false,
  "error": {
    "code": "CONFLICT",
    "message": "User has already booked this event"
  }
}
```

**409 - No Available Seats:**
```json
{
  "success": false,
  "error": {
    "code": "CONFLICT",
    "message": "No available seats for this event"
  }
}
```

## Тестирование API с Postman

### Шаг 1: Добавьте тестовое мероприятие

Подключитесь к PostgreSQL и выполните:

```sql
INSERT INTO events (name, total_seats) VALUES ('Концерт', 100);
INSERT INTO events (name, total_seats) VALUES ('Конференция', 50);
INSERT INTO events (name, total_seats) VALUES ('Вебинар', 1);
```

### Шаг 2: Запустите сервер

```bash
npm run dev
```

Сервер будет доступен по адресу: `http://localhost:3000`

### Шаг 3: Тестовые запросы в Postman

#### Тест 1: Успешное бронирование

**Method:** POST  
**URL:** `http://localhost:3000/api/bookings/reserve`  
**Headers:**
```
Content-Type: application/json
```
**Body (raw JSON):**
```json
{
  "event_id": 1,
  "user_id": "user123"
}
```

**Ожидаемый результат:** 201 Created с данными бронирования

#### Тест 2: Дублирование бронирования

Повторите Тест 1 с теми же данными.

**Ожидаемый результат:** 409 Conflict с сообщением о дубликате

#### Тест 3: Несуществующее мероприятие

**Body:**
```json
{
  "event_id": 999999,
  "user_id": "user456"
}
```

**Ожидаемый результат:** 404 Not Found

#### Тест 4: Отсутствие обязательных полей

**Body:**
```json
{
  "event_id": 1
}
```

**Ожидаемый результат:** 400 Bad Request

#### Тест 5: Превышение вместимости

Забронируйте все места на событие с `event_id: 3` (1 место), затем попробуйте забронировать еще раз с другим user_id.

**Ожидаемый результат:** 409 Conflict с сообщением о нехватке мест

### Шаг 4: Проверка данных в БД

```sql
-- Просмотр всех бронирований
SELECT * FROM bookings;

-- Просмотр доступных мест
SELECT 
  e.id,
  e.name,
  e.total_seats,
  COUNT(b.id) as booked,
  e.total_seats - COUNT(b.id) as available
FROM events e
LEFT JOIN bookings b ON e.id = b.event_id
GROUP BY e.id, e.name, e.total_seats;

-- Очистка данных для повторного тестирования
TRUNCATE TABLE bookings RESTART IDENTITY CASCADE;
```

## Архитектура

Проект следует принципам SOLID и разделен на слои:

- **Controllers** - обработка HTTP запросов
- **Services** - бизнес-логика и транзакции
- **Repositories** - работа с базой данных
- **Models** - типы данных и модели

## Особенности реализации

- ✅ Транзакции с пессимистическими блокировками (SELECT FOR UPDATE)
- ✅ Защита от race conditions при конкурентных запросах
- ✅ Валидация входных данных
- ✅ Контроль вместимости мероприятий
- ✅ Предотвращение дублирования бронирований
- ✅ Property-based тестирование (100+ итераций на свойство)
- ✅ Integration тесты для end-to-end проверки

## 📁 Структура проекта

```
booking-system/
├── src/
│   ├── controllers/      # HTTP контроллеры
│   ├── services/         # Бизнес-логика
│   ├── repositories/     # Доступ к данным
│   ├── models/           # Типы и модели
│   ├── database/         # Подключение и миграции
│   ├── middleware/       # Express middleware
│   ├── config/           # Конфигурация
│   ├── routes/           # Маршруты API
│   └── app.ts            # Точка входа
└── scripts/              # Утилиты
```

## 📄 License

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 👨‍💻 Ternopolskiy

Создано для демонстрации профессиональной разработки REST API с TypeScript, PostgreSQL и property-based testing.

